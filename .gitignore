# secrets / local config
.env
.env.*
!.env.example

# tauri / rust
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/.tmp/

# local, machine-specific cargo build config (target-dir + perl path)
.cargo/

# local data (lives in %APPDATA% anyway, belt and braces)
*.db
*.db-journal

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# SDD scratch workspace
.superpowers/
RESUME.md

# Playwright MCP scratch (render checks)
.playwright-mcp/
